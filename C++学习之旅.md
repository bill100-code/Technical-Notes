# C++学习之旅

## 一、继承

class Base{};

class JAVA : public Base

{

}// 通过这种方式实现继承

### 继承的三种形式

继承共有三种形式，public,private和protected

1.使用public继承后，父类的public仍然是public，proteced仍然是proteced，private无法继承

2.使用protected继承，所有可访问的均变为protected

3.使用private,public与protected都变成子类的private

### 构造函数与析构函数的顺序

继承中先调用父类构造函数，再调用子类构造函数，析构的顺序与之相反

### 继承同名成员的处理方法

#### 同名的成员属性

1.如果出现同名,利用.访问会得到自身的数据

2.如果要访问父类的同名成员，加上父类作用域即可

例：cout << son.base::m_a; 

#### 同名的成员函数

与上面一致，调用自己的直接.，调用父类的写上作用域

如果子类中出现了同名的成员函数，会隐藏父类的所有同名成员函数，只能加上作用域，才能访问到父类中的隐藏的童名成员函数

### 继承同名静态成员处理方法

静态成员的特点：编译阶段分配内存，所有静态对象共享一片内存

静态数据有两种访问方式：

1.通过对象进行访问， cout << s.ma;  cout<<s.Base::ma;

2.通过类名直接访问，cout<<Base::ma;

子类仍然会隐藏父类