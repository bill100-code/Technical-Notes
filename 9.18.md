# GCC+GDB调试入门

## 主题：windows下vscode调试，GDB基本与进阶用法，GCC/GDB前缀含义，断点管理

### 1.windows下GDB环境准备

vscode本身不带GDB,只是调用系统中的GDB

#### 1.1获得版本信息

gcc --version

gdb--version

如果没有显示出来，则说明没有安装GDB，或者没有配置环境变量

#### 2.编译

GDB调试需要调试信息，编译命令：

gcc -g -O0 -Wall -DDEBUG -o test.exe test.c

-g:生成调试信息

-O0:关闭优化，避免变量被优化掉

-Wall 打开常用警告

-DDEBUG 定义DEBUG宏，让DEBUG生效

-o test.exe 输出文件名

-S生成汇编文件

-c只编译不链接

-E预处理

#### 3.GDB调试

进入GDB：

gdb -q test.exe

-q表示安静地打开，不显示版本信息等

break 设置断点，可以缩写为b

break main

break 20



run运行程序,缩写为r

next 但不跳过，不进入函数,n

step，单步进入函数，s

continue,继续到下一个断点,c

print 变量

info locals 查看当前函数局部变量

backtrace 查看调用栈，bt

frame 编号 切换栈帧, f编号

watch 变量 变量改变时暂停

quit q 退出



#### 4.查看寄存器

info registers 显示通用寄存器

info all-registers 显示所有寄存器，包括xmm,标志寄存器等

单独查看：

p/x $ray



#### 5.查看内存

x/数量 格式 单位 地址

x十六进制     d十进制     u无符号十进制   f浮点 s字符串   i反汇编指令

b 1字节   h  2字节    w   4字节    g   8字节 